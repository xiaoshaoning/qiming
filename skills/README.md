# MCP Skills

Place MCP skill markdown files here. Each skill is a reusable Agent workflow:

- `auto_debug.md` — from failure to root-cause diagnosis
- `coverage_gap_fill.md` — analyze uncovered branches, generate testbenches
- `interface_check.md` — verify port connections and width matching

Skills are loaded on demand by AI agents via the Skill tool.
